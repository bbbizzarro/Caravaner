using System;
using Godot;

public class Simulator {
    public void Simulate(World world, float delta) { 
		//for (int x = 0; x < world.GetWidth(); ++x) {
		//	for (int y = 0; y < world.GetHeight(); ++y) {
		//		Tile tile = world.GetTile(x, y);
		//		if (tile != null && tile.visible) {
		//			tile.debugValue = (tile.debugValue + delta) % 3;
		//			tile.type = Mathf.RoundToInt(tile.debugValue);
		//		}
		//	}
		//}
	}
}
